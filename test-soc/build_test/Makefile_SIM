TEST_DIR 		:= $(abspath $(ARM_IP_DIR)/software/testcodes/$(TESTNAME))
CUR_DIR 		:= $(abspath .)

all: rom_image

rom_image:
	cd $(TEST_DIR) ; \
	$(MAKE) ;
    cp $(TEST_DIR)/$(TESTNAME).hex outputs/
	cd $(TEST_DIR) ; \
	$(MAKE) clean ;
